# Solana Network Configuration
# Options: devnet, mainnet, testnet, localhost
VITE_SOLANA_NETWORK=devnet

# Custom RPC Endpoint (optional - overrides network setting)
# VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# Local validator endpoint (when using localhost)
# VITE_LOCAL_RPC_URL=http://localhost:8899
